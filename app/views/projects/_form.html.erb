<%= render('layouts/errors', :object => @projects) %>

  <%= form_for [@language, @project] do |f| %>
    <%= f.label :description %>
    <%= f.text_field :description %>
    <p><%= f.submit %></p>
  <% end %>
  